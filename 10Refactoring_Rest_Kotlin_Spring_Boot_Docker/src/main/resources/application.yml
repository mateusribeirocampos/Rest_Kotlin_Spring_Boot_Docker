spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_campos?useTimezone=true&serverTimezone=UTC
    username: root
    password: Admin123#
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      jakarta:
        persistence:
          database-platform: org.hibernate.dialect.MySQLDialect