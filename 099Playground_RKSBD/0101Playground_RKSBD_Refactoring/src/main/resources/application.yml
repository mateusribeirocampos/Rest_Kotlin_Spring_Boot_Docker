spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/playground_test_refact?useTimezone=true&serverTimezone=UTC
    username: root
    password: Admin123#
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      jakarta:
        persistence:
          database-platform: org.hibernate.dialect.MySQL8Dialect
